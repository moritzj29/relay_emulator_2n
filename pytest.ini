[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Exclude the manual integration test script (test_relay.py at root)
# Only run tests in the tests/ directory
norecursedirs = .git .venv relay_emulator_2n
